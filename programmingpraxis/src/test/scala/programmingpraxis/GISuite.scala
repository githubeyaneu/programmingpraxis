package programmingpraxis

import org.junit.runner.RunWith
import org.scalatest.junit.JUnitRunner
import programmingpraxis._

@RunWith(classOf[JUnitRunner])
class GISuite {

  type GI = GaussianInteger
  //assert(GaussianInteger(0, 0).quotient(GaussianInteger(0, 0)) === GaussianInteger(0, 0))
}